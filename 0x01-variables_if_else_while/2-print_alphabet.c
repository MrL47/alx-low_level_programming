#include <stdio.h>

/**
 * main - insomnia
 * Description: the alphabet game
 * retun (0)
 */

int main(void);

{
	char alphabet;
	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
		putchar(alphabete);
	putchar('n');
	return (0);
}
